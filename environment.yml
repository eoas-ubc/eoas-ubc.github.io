name: jupyterbook
channels:
  - eoas_ubc
  - conda-forge
  - defaults
dependencies:
  - git
  - python=3.8
  - pip
  - ghp-import
  - jupytext
  - jupyter-book
  - conda-lock
  - mamba
# conda-lock --file environment.yml -p linux-64
